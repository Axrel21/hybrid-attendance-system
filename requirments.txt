# DEPRECATED — kept for legacy muscle memory (note: filename typo).
#
# This file used to be the only requirements list (pre-split). The canonical
# pins are now bundle-specific:
#
#   Edge / Raspberry Pi:  pip install -r edge/requirements-edge.txt
#   Cloud / ArcFace srv:  pip install -r cloud/requirements.txt
#
# Forward to the edge stack, since the historical content of this file
# (numpy, opencv-python-headless, scipy, psutil, the now-removed
# tensorflow-cpu) was the on-device dependency set.
#
# pip install -r requirments.txt
#   == pip install -r edge/requirements-edge.txt

-r edge/requirements-edge.txt
